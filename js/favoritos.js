document.addEventListener('DOMContentLoaded',()=>{const grid=document.querySelector('#favoritesGrid');if(!grid)return;const summary=document.querySelector('#favoritesSummary');const draw=()=>{const ids=new Set(GLT.state.favorites.map(Number));const list=PRODUTOS.filter(p=>ids.has(Number(p.id)));if(summary)summary.textContent=`${list.length} ${list.length===1?'ITEM FAVORITADO':'ITENS FAVORITADOS'}`;grid.innerHTML=list.length?list.map(GLT.card).join(''):'<div class="empty"><h2>SUA SELEÇÃO ESTÁ VAZIA.</h2><p>Marque os produtos que fazem parte do seu jogo.</p><a class="btn" href="produtos.html">EXPLORAR CATÁLOGO</a></div>'};draw();document.addEventListener('gltFavorites',draw)});
