document.addEventListener('DOMContentLoaded',()=>{const grid=document.querySelector('#favoritesGrid');if(!grid)return;const draw=()=>{const list=PRODUTOS.filter(p=>GLT.state.favorites.includes(p.id));grid.innerHTML=list.length?list.map(GLT.card).join(''):'<div class="empty"><h2>SUA SELEÇÃO ESTÁ VAZIA.</h2><p>Marque os produtos que fazem parte do seu jogo.</p><a class="btn" href="produtos.html">EXPLORAR CATÁLOGO</a></div>'};draw();document.addEventListener('gltFavorites',draw)});
